export enum Path {
    ANY = "*",
    MAIN = "/",
    LOGIN = "/login",
    REGISTER = "/register",
    LOGOUT = "/logout",

    PROFILE = "/:username",

    SETTINGS = "/settings",
    SETTINGS_PROFILE = "/profile",
}